<!-- ───────── SIDEBAR NAV ───────── -->
<nav class="sidebar" id="sidebar">
    <div class="sidebar-logo">
        <span class="logo-icon">🚦</span>
        <span class="logo-text">ПДД<br><small>Экзаменатор</small></span>
    </div>
    <ul class="nav-links">
        <li>
            <a asp-controller="Home" asp-action="index" class="nav-link active" data-page="home">
                <span class="nav-icon">⬡</span> Главная
            </a>
        </li>
        <li>
            <a asp-controller="Home" asp-action="exam" class="nav-link" data-page="exam">
                <span class="nav-icon">◈</span> Экзамен
            </a>
        </li>
        <li>
            <a asp-controller="Home" asp-action="train" class="nav-link" data-page="train">
                <span class="nav-icon">◉</span> Тренировка
            </a>
        </li>
        <li>
            <a asp-controller="Mistakes" asp-action="Index" class="nav-link" data-page="mistakes">
                <span class="nav-icon">◪</span> Ошибки
            </a>
        </li>
        <li>
            <a asp-controller="Home"  asp-action="stats" class="nav-link" data-page="stats">
                <span class="nav-icon">◧</span> Статистика
            </a>
        </li>
        @if (User.IsInRole("Admin"))
        {
            
        <li class="nav-item-dropdown">
    <div class="nav-link nav-dropdown-toggle" onclick="toggleAdminMenu(this)">
        <span class="nav-icon">♛</span>
        <span>Администрирование</span>
        <span class="nav-arrow">›</span>
    </div>
    <ul class="nav-submenu">
        <li>
            <a asp-controller="Admin" asp-action="EditTicket" class="nav-sublink">
                <span class="sub-icon">◈</span> Билеты
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="EditTheme" class="nav-sublink">
                <span class="sub-icon">◉</span> Темы
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="EditUser" class="nav-sublink">
                <span class="sub-icon">◎</span> Пользователи
            </a>
        </li>
    </ul>
</li>

        }
    </ul>
    <div class="sidebar-bottom">
        @if (User.Identity.IsAuthenticated)
        {
            <div class="auth-block user-state">
                <div class="user-card">
                    <div class="user-avatar">@User.Identity.Name.Substring(0, 2).ToUpper()</div>
                    <div class="user-info">
                        <span class="user-name">@User.Identity.Name</span>
                    </div>
                </div>
                <a asp-controller="Account" asp-action="Logout" class="sidebar-logout">Выйти</a>
            </div>
        }
        else
        {
            <div class="auth-block guest-state">
                <a asp-controller="Account" asp-action="Authorization" class="btn-sidebar-login">Войти</a>
            </div>
        }
    </div>
</nav>